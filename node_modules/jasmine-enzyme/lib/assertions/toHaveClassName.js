'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _negateMessage = require('../negateMessage');

var _negateMessage2 = _interopRequireDefault(_negateMessage);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = {
  toHaveClassName: function toHaveClassName() {
    function toHaveClassName(enzymeWrapper, className) {
      var normalizedClassName = className.split(' ').join('.');

      if (normalizedClassName[0] !== '.') {
        normalizedClassName = '.' + normalizedClassName;
      }

      return {
        pass: enzymeWrapper.is(normalizedClassName),
        message: 'Expected "' + enzymeWrapper.html() + '" to have className of ' + className + ' but instead found ' + enzymeWrapper.props('className') };
    }

    // eslint-disable-line max-len
    return {
      compare: function compare(enzymeWrapper, className) {
        return toHaveClassName(enzymeWrapper, className);
      },
      negativeCompare: function negativeCompare(enzymeWrapper, className) {
        var result = toHaveClassName(enzymeWrapper, className);

        result.message = (0, _negateMessage2.default)(result.message);
        result.pass = !result.pass;

        return result;
      }
    };
  }
}; /**
    * This source code is licensed under the MIT-style license found in the
    * LICENSE file in the root directory of this source tree. *
    *
    * @providesModule toHaveClassName
    * 
    */

module.exports = exports['default'];