'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _negateMessage = require('../negateMessage');

var _negateMessage2 = _interopRequireDefault(_negateMessage);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = {
  toHaveHTML: function toHaveHTML() {
    function toHaveHTML(enzymeWrapper, html) {
      var wrapperHTML = enzymeWrapper.html();

      // normalize quotes
      var useSingleQuotes = html.search("'") !== -1;

      var actualHTML = wrapperHTML.replace(/("|')/g, useSingleQuotes ? "'" : '"');
      var expectedHTML = html.replace(/("|')/g, useSingleQuotes ? "'" : '"');

      return {
        pass: actualHTML === expectedHTML,
        message: 'Expected "' + actualHTML + '" to equal ' + expectedHTML
      };
    }

    return {
      compare: function compare(enzymeWrapper, html) {
        return toHaveHTML(enzymeWrapper, html);
      },
      negativeCompare: function negativeCompare(enzymeWrapper, html) {
        var result = toHaveHTML(enzymeWrapper, html);

        result.message = (0, _negateMessage2.default)(result.message);
        result.pass = !result.pass;

        return result;
      }
    };
  }
}; /**
    * This source code is licensed under the MIT-style license found in the
    * LICENSE file in the root directory of this source tree. *
    *
    * @providesModule toHaveHTML
    * 
    */

module.exports = exports['default'];